// import { Database } from '../src/infra/mikroOrm/config/mikro-orm.config';
// import expressApp from "./httpServer/config/config";

// export default async function startServer() {
//     try {
//         await Database.init();  // ✅ Initialize Database first
//         expressApp.listen(3000, () => console.log("🚀 Server running on port 3000"));
//     } catch (error) {
//         console.error("❌ Error starting server:", error);
//         process.exit(1); // Exit if database fails
//     }
// }
